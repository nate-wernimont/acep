<benchmark>
        <class>JGFMolDynBenchSizeA</class>	<!-- the mainclass that contains main method -->
        <method>main</method>	<!-- the entry point method (change it if not main) -->
        <processdir>
                <dir>./moldyn</dir>	<!-- directory which contains config.xml and lib -->
        </processdir>
        <includes>
                <inc>jgfutil</inc>	<!-- based on the imports in the program, if some packages have to be analyzed -->
                <inc>moldyn</inc>
        </includes>
        <lib>./moldyn/lib</lib>	<!-- lib folder location -->
        <jre>/usr/java/jdk1.6.0_45/jre/lib</jre>	<!-- jre location, basically this location needs to contain rt.jar and jce.jar -->
        <jars>
                <jar>JGMolDyn.jar</jar>	<!-- program jar file containing all class files to be analyzed -->
        </jars>
</benchmark>